import { NextPage } from "next";

const OneProject: NextPage = () => {
  return (
    <main className="hidden bg-dots lg:flex flex-col justify-start items-center w-full h-full bg-white"></main>
  );
};

export default OneProject;
